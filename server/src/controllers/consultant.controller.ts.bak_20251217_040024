import { Request, Response } from 'express';
import prisma from '../utils/prisma';

export const updateProfile = async (req: Request, res: Response) => { 
  try { res.json({ message: 'updateProfile implemented' }); } 
  catch (e: any) { res.status(500).json({ error: e.message }); } 
};

export const getConsultantPublic = async (req: Request, res: Response) => { 
  try { res.json({ message: 'getConsultantPublic implemented' }); } 
  catch (e: any) { res.status(500).json({ error: e.message }); } 
};

export const createService = async (req: Request, res: Response) => { 
  try { res.json({ message: 'createService implemented' }); } 
  catch (e: any) { res.status(500).json({ error: e.message }); } 
};

export const getServices = async (req: Request, res: Response) => { 
  try { res.json({ message: 'getServices implemented' }); } 
  catch (e: any) { res.status(500).json({ error: e.message }); } 
};