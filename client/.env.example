# API服务器地址
VITE_API_URL=http://localhost:3000/api

# WebSocket服务器地址
VITE_WS_URL=http://localhost:3000

# Optional: force browser-local portfolio authentication without a backend.
# If omitted, production enables it when VITE_API_URL is missing or uses localhost.
# VITE_DEMO_MODE=true
